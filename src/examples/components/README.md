# Component Examples

These `.example.astro` files show component variants and expected usage.

They are intentionally stored outside `src/pages/`, so they are not routed or deployed by Astro. Import them into a temporary local preview page or component-workbench route during development, then remove that route before shipping.

Keep examples updated when component attributes or variants change.
